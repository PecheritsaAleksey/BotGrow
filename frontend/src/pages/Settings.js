import { jsx as _jsx } from 'react/jsx-runtime';
import Card from '@/components/Card';
export default function Settings() {
  return _jsx(Card, {
    title: 'Settings',
    children: _jsx('p', { children: 'Settings page content.' }),
  });
}
